# Installation


To host mrazota on Linux run:

```
git clone https://github.com/dead-lamer/MrazotaSpace.git

cd MrazotaSpace

chmod +x install.sh

./install.sh
```

Then run it with:

```
sudo node app.js
```
